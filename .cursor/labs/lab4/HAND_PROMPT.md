# Lab 4 hand-typed prompt

Paste this into Agent chat as-is (do not also invoke `/lab4-status`):

```
Check the .cursor rules and labs folders and write a short status under .cursor/labs/lab4/.
```

Run that three times in fresh chats (or clear prior status output between runs). Save each resulting file as:

- `hand-1.md` (copy of whatever the agent wrote)
- `hand-2.md`
- `hand-3.md`
