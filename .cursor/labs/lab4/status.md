# Lab 4 status

Checked `.cursor/rules` (direct `*.mdc` only) and `.cursor/labs` (recursive).

rules_mdc: 6
labs_files: 11

Rules: lab1-demo-mdc-extension-name-reply, lab2-agent-requested, lab2-always, lab2-auto-attached, lab2-manual, lab3-scoped.

Labs: `outside.md`, `lab3/inside.md`, lab4 notes (`HAND_PROMPT.md`, `SCORECARD.md`, `status.md`), and six snapshots under `runs/`.
