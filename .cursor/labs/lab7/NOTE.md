# Lab 7 · beforeFileEdit deny

Protected file: `.cursor/labs/lab7/probe.md`

Hook: `.cursor/hooks/lab7-deny-edit.sh` via `beforeFileEdit` in `.cursor/hooks.json`.

Return shape on deny: `{"permission":"deny","userMessage":"..."}`
